---
import Layout from "../../components/layout.astro";
import Button from "../../components/ui/button.astro";
import Card from "../../components/ui/card.astro";
import Title from "../../components/ui/title.astro";
import { URLS } from "../../lib/URL-helpers";
import { ArrowRight, KeyRound, LayoutTemplate, Workflow, AlertTriangle } from "@lucide/astro";
import { getStaticLocalePaths, t, localePath, type Locale } from "../../i18n";

export const getStaticPaths = getStaticLocalePaths;
const lang = Astro.params.lang as Locale;
---

<Layout
  lang={lang}
  title={t(lang, "pages.demo.title")}
  description={t(lang, "pages.demo.description")}
>
  <section class="pt-32 pb-16">
    <div class="mx-auto max-w-3xl px-4 text-center">
      <p class="eyebrow">{t(lang, "demo.eyebrow")}</p>
      <Title level={1} class="mt-6">{t(lang, "demo.title")}</Title>
      <p class="mt-4 text-lg text-[var(--muted-text)]">
        {t(lang, "demo.description")}
      </p>
    </div>
  </section>

  <section class="pb-20">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">

      <!-- Login instructions -->
      <Card variant="brand" radius="3xl" shadow="md" padding="lg" class="mx-auto max-w-3xl mb-12">
        <div class="flex items-start gap-4">
          <KeyRound class="w-6 h-6 mt-0.5 shrink-0 text-[var(--color-peach-paper-accent)]" />
          <div>
            <p class="font-semibold text-[var(--color-peach-paper-heading)]">{t(lang, "demo.loginCard.title")}</p>
            <p class="mt-1 text-[var(--color-peach-paper-copy)]">
              {t(lang, "demo.loginCard.description")}
            </p>
          </div>
        </div>
      </Card>

      <!-- Two app cards -->
      <div class="grid gap-8 md:grid-cols-2 max-w-5xl mx-auto">

        <!-- Epistola Suite -->
        <Card variant="default" radius="3xl" shadow="md" padding="lg" class="flex flex-col">
          <div class="flex items-center gap-3 mb-4">
            <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-[var(--color-peach-paper-accent)] text-white">
              <LayoutTemplate class="w-5 h-5" />
            </div>
            <div>
              <p class="text-xs uppercase tracking-[0.14em] font-mono text-[var(--color-peach-paper-chip-text)]">
                {t(lang, "demo.suite.label")}
              </p>
            </div>
          </div>
          <Title level={3} class="text-xl">{t(lang, "demo.suite.title")}</Title>
          <p class="mt-3 text-[var(--color-peach-paper-copy)] leading-relaxed flex-1">
            {t(lang, "demo.suite.description")}
          </p>
          <div class="mt-6">
            <Button href={URLS.DEMO} variant="primary" size="md" external class="group">
              {t(lang, "demo.suite.cta")}
              <ArrowRight class="w-4 h-4 transition-transform duration-200 group-hover:translate-x-1" />
            </Button>
          </div>
        </Card>

        <!-- Valtimo Plugin -->
        <Card variant="default" radius="3xl" shadow="md" padding="lg" class="flex flex-col">
          <div class="flex items-center gap-3 mb-4">
            <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-[var(--color-peach-paper-accent)] text-white">
              <Workflow class="w-5 h-5" />
            </div>
            <div>
              <p class="text-xs uppercase tracking-[0.14em] font-mono text-[var(--color-peach-paper-chip-text)]">
                {t(lang, "demo.valtimo.label")}
              </p>
            </div>
          </div>
          <Title level={3} class="text-xl">{t(lang, "demo.valtimo.title")}</Title>
          <p class="mt-3 text-[var(--color-peach-paper-copy)] leading-relaxed flex-1">
            {t(lang, "demo.valtimo.description")}
          </p>
          <div class="mt-6">
            <Button href={URLS.VALTIMO_DEMO} variant="secondary" size="md" external class="group">
              {t(lang, "demo.valtimo.cta")}
              <ArrowRight class="w-4 h-4 transition-transform duration-200 group-hover:translate-x-1" />
            </Button>
          </div>
        </Card>
      </div>

      <!-- Disclaimer -->
      <div class="mx-auto max-w-3xl mt-12 flex items-start gap-3 px-4 py-3 text-sm text-[var(--color-peach-paper-chip-text)]">
        <AlertTriangle class="w-4 h-4 mt-0.5 shrink-0" />
        <p>
          {t(lang, "demo.disclaimer")}
        </p>
      </div>

    </div>
  </section>

  <section class="py-24">
    <Card variant="brand" radius="4xl" shadow="lg" padding="none" class="mx-auto max-w-4xl px-6 py-16 text-center">
      <Title level={2}>{t(lang, "demo.cta.title")}</Title>
      <p class="mt-4 text-lg text-[var(--muted-text)]">
        {t(lang, "demo.cta.description")}
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <Button href={localePath(lang, "/contact")} variant="primary" size="md">{t(lang, "demo.cta.getInTouch")}</Button>
        <Button href={localePath(lang, "/pricing")} variant="secondary" size="md">{t(lang, "demo.cta.reviewPlans")}</Button>
      </div>
    </Card>
  </section>
</Layout>
