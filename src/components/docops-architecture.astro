---
const lanes = [
  {
    title: "Business authors",
    body: "Drag/drop blocks, manage schema examples, and preview PDFs with inline expression evaluation.",
    bullets: [
      "Block library: text, tables, columns, clauses, headers, footers",
      "Undo/redo + autosave stacks",
      "Inline JSONata + GraalJS expression tester",
    ],
  },
  {
    title: "Developers",
    body: "Version templates in Git, run Testcontainers suites, and invoke the OpenAPI-render API from orchestrators.",
    bullets: [
      "CQRS commands for template + theme automation",
      "Adaptive batch jobs with job states and metrics",
      "Webhooks + storage adapters (S3, Azure, DMS)",
    ],
  },
  {
    title: "Platform ops",
    body: "Deploy Helm charts with Keycloak/OIDC, monitor renders, and promote versions across environments.",
    bullets: [
      "Multi-tenant isolation + RBAC memberships",
      "Environments per tenant (staging/production)",
      "Prometheus/OTel metrics + health probes",
    ],
  },
];

const pipeline = [
  {
    title: "Template service",
    note: "Variants · versions · JSON Schema · theming",
  },
  {
    title: "Generation engine",
    note: "iText Core · expression sandbox · token cascade",
  },
  {
    title: "Delivery",
    note: "Jobs API · storage bridges · problem+json errors",
  },
];
---

<section class="py-24">
  <div class="mx-auto max-w-6xl px-4">
    <div class="grid gap-12 lg:grid-cols-[1.1fr_0.9fr]">
      <div>
        <p class="eyebrow">DocOps architecture</p>
        <h2 class="mt-4 text-4xl font-semibold text-[var(--color-gray-900)] dark:text-white">
          Everything from editor to render API runs on the same opinionated stack.
        </h2>
        <p class="mt-4 text-lg text-[var(--muted-text)]">
          Epistola Suite isn’t a generic CMS. It is a multi-tenant document platform: Lit/ProseMirror editor, template service built on CQRS, iText renderer, and REST/Helm deployment tooling. Each persona plugs into the layer they need without reimplementing the stack.
        </p>
        <div class="mt-10 space-y-6">
          {lanes.map((lane) => (
            <div class="surface-card rounded-3xl border border-black/5 bg-white/80 p-6 shadow-sm dark:bg-white/5">
              <p class="text-xs font-semibold uppercase tracking-[0.3em] text-[var(--color-brand-500)]">{lane.title}</p>
              <p class="mt-3 text-sm text-[var(--muted-text)]">{lane.body}</p>
              <ul class="mt-4 space-y-2 text-sm text-[var(--color-gray-700)] dark:text-gray-300">
                {lane.bullets.map((bullet) => (
                  <li class="flex gap-2">
                    <span aria-hidden="true">•</span>
                    <span>{bullet}</span>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
      <div>
        <div class="surface-card relative overflow-hidden rounded-[32px] border border-black/10 bg-white/95 p-8 shadow-xl dark:bg-[rgba(15,23,42,.95)]">
          <div class="absolute -top-16 right-0 h-32 w-32 rounded-full bg-[var(--color-brand-100)] blur-3xl"></div>
          <div class="relative">
            <p class="text-xs font-semibold uppercase tracking-[0.4em] text-[var(--color-gray-400)]">Render pipeline</p>
            <h3 class="mt-3 text-2xl font-semibold text-[var(--color-gray-900)] dark:text-white">Data ➝ Template ➝ PDF</h3>
            <div class="mt-6 space-y-6">
              {pipeline.map((stage, index) => (
                <div class="relative rounded-2xl border border-black/10 bg-[var(--color-gray-50)]/80 p-5 dark:bg-black/20">
                  <div class="flex items-center justify-between">
                    <p class="text-sm font-semibold text-[var(--color-gray-900)] dark:text-white">{stage.title}</p>
                    <span class="text-xs font-semibold text-[var(--color-brand-600)]">Step {index + 1}</span>
                  </div>
                  <p class="mt-3 text-sm text-[var(--muted-text)]">{stage.note}</p>
                  {index < pipeline.length - 1 && (
                    <div class="mt-5 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.3em] text-[var(--color-gray-400)]">
                      Next
                      <span aria-hidden="true">→</span>
                    </div>
                  )}
                </div>
              ))}
            </div>
            <div class="mt-8 rounded-2xl border border-[var(--color-brand-100)] bg-[var(--color-brand-50)]/70 p-5 text-sm text-[var(--color-brand-900)] dark:bg-[rgba(59,130,246,.15)] dark:text-[var(--color-brand-50)]">
              <p class="font-semibold">Observability</p>
              <p class="mt-2">Adaptive batch logging, Prometheus counters, and structured problem+json errors make renders auditable in any SIEM.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
